# 010-全中文界面前备份

## 备份时间
- 2026-06-29

## 本阶段目标
- 将软件运行界面中出现的英文文本统一改为中文。
- 重点修复 AUSBC 底层英文错误信息原样显示的问题。
- 将实时帧率显示从 `fps` 改为中文。
- 将默认设备名 `USB/UVC` 改为中文。
- 重新构建 APK，并按发布规则将版本从 `1.1.0` 升到 `1.2.0`。

## 当前修改到哪个模块
- 模块十：全中文界面。

## 修改前关键代码

### 底层错误信息原样展示
```kotlin
CameraStatus.ERROR_PREVIEW_SIZE -> {
    revealControls()
    uiState.copy(
        connectionState = DeviceConnectionState.Connecting,
        statusText = "正在匹配可用分辨率",
        errorMessage = status.message,
        previewSizes = details.previewSizes
    )
}
```

### 默认设备名
```kotlin
if (device == null) return "USB/UVC"
return device.productName ?: "USB/UVC"
```

### 帧率显示
```kotlin
text = "${uiState.captureFps} fps"
Text("${frameRate}fps", fontSize = 12.sp)
```

### 版本号
```kotlin
versionCode = 2
versionName = "1.1.0"
```

## 待办清单
- 修改 `MainActivity.kt`。
- 修改 `app/build.gradle.kts`。
- 执行 `.\gradlew.bat :app:assembleDebug`。
- 确认 `USB采集卡.apk` 版本为 `1.2.0`。
- 清理项目根目录 `.gradle` 临时缓存。
- 生成 `进度快照/011-全中文界面.md`。
