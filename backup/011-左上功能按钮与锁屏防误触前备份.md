# 011-左上功能按钮与锁屏防误触前备份

## 备份时间
- 2026-06-29

## 本阶段目标
- 将功能按钮从右下角移动到左上角。
- 全屏预览时点击画面，在左上角半透明显示功能按钮。
- 无操作 3 秒后自动隐藏功能按钮。
- 右侧屏幕边缘中部增加锁定屏幕按钮。
- 锁定后避免误操作；再次点击锁图标解除锁定。
- 重新构建 APK，并按版本规则从 `1.2.0` 升到 `1.3.0`。

## 当前修改到哪个模块
- 模块十一：左上功能按钮与锁屏防误触。

## 修改前关键代码

### 功能按钮位置
```kotlin
Box(
    modifier = Modifier
        .fillMaxSize()
        .padding(horizontal = 18.dp, vertical = 16.dp),
    contentAlignment = Alignment.BottomEnd
)
```

### 预览返回键显示/隐藏
```kotlin
if (uiState.isPreviewing) {
    if (showControls) {
        hideControlsForPreview()
    } else {
        revealControls()
    }
    return
}
```

### 版本号
```kotlin
versionCode = 3
versionName = "1.2.0"
```

## 待办清单
- 修改 `MainActivity.kt`。
- 修改 `app/build.gradle.kts`。
- 执行 `.\gradlew.bat :app:assembleDebug`。
- 确认 `USB采集卡.apk` 版本为 `1.3.0`。
- 清理项目根目录 `.gradle` 临时缓存。
- 生成 `进度快照/012-左上功能按钮与锁屏防误触.md`。
