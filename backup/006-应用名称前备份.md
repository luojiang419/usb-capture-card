# 006-应用名称前备份

## 阶段
应用名称修改开始前。

## 当前状态
- 最新功能模块为 `006-自动横竖屏适配`。
- APK 可正常构建。
- Manifest 中应用名称使用 `@string/app_name`。
- 当前 `app_name` 为 `USB Capture`。

## 本阶段计划修改
- 仅修改应用显示名称为 `USB采集卡`。
- 重新执行 clean 构建，生成最新 APK。

## 代码前后对比

### 修改前
```xml
<string name="app_name">USB Capture</string>
```

### 修改后预期
```xml
<string name="app_name">USB采集卡</string>
```

## 待办清单
- 修改 `strings.xml`。
- 执行 `.\gradlew.bat clean :app:assembleDebug`。
- 生成新的进度快照。

## 下一步
修改应用名称并重新编译 APK。
