# 008-分辨率自动检测与实时帧率前备份

## 备份时间
- 2026-06-29

## 本阶段目标
- 在采集设置里增加“可用分辨率自动检测”选项，解决当前只显示 `1280 x 720` 的问题。
- 在采集画面右上角增加半透明实时采集帧速率显示。

## 当前修改到哪个模块
- 模块八：分辨率自动检测与实时 FPS 显示。

## 修改前关键代码

### 分辨率列表采集
```kotlin
val sizes = cameraClient?.getAllPreviewSizes()
    ?.map { PreviewOption(it.width, it.height) }
    ?.distinct()
    .orEmpty()
```

### 采集设置分辨率入口
```kotlin
ResolutionSelector(
    options = uiState.previewSizes,
    current = uiState.previewSizeText,
    onResolutionSelected = onResolutionSelected
)
```

### UI 状态
```kotlin
val previewSizes: List<PreviewOption> = emptyList(),
val selectedFrameRate: Int = DEFAULT_FRAME_RATE,
val isPreviewing: Boolean = false,
```

## 预计修改后的结构
- 新增 `detectPreviewSizes()`，尝试多个比例参数读取 AUSBC 可用分辨率并去重排序。
- 新增“自动检测”按钮，点击后刷新 `uiState.previewSizes`。
- 新增 `IPreviewDataCallBack` 统计实时预览帧回调，每秒更新 `captureFps`。
- 新增右上角半透明 FPS 浮层，仅预览时显示。

## 待办清单
- 修改 `MainActivity.kt`。
- 执行 `.\gradlew.bat :app:assembleDebug`。
- 清理项目根目录 `.gradle` 临时缓存。
- 生成 `进度快照/009-分辨率自动检测与实时帧率.md`。
