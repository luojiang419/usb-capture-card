# 002-主题与动效前备份

## 阶段
模块二：主题与动效基础开始前。

## 当前状态
- 模块一已完成，项目可通过 `.\gradlew.bat :app:assembleDebug` 编译。
- 当前 UI 是极简 `TextView` 状态提示层，仅用于骨架验证。

## 本阶段计划修改
- 使用 `ComposeView` 作为视频预览上方的控制层。
- 实现 Material 3 浅色、暗黑、跟随系统主题。
- 实现主题切换持久化。
- 实现连接状态、无设备、录制入口、设置面板等基础动效。

## 代码前后对比

### 修改前：极简状态文字
```kotlin
statusText = TextView(this).apply {
    text = "等待 USB 采集卡"
    textSize = 18f
}
root.addView(statusText, ...)
```

### 修改后预期：Compose 控制层
```kotlin
composeOverlay = ComposeView(this).apply {
    setContent {
        UsbCaptureTheme(themeMode) {
            UsbCaptureScreen(...)
        }
    }
}
root.addView(composeOverlay, ...)
```

## 待办清单
- 替换状态文字层为 Compose UI。
- 添加主题模型和持久化。
- 添加基础状态转场和按钮动画。
- 构建验证。

## 下一步
修改 `MainActivity.kt`，实现主题与动效基础。
