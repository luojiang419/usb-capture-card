# 004-录制文件前备份

## 阶段
模块四：录制与文件开始前。

## 当前状态
- 模块三已完成，应用支持运行时权限、USB 插拔状态、设备信息和分辨率切换。
- 录制按钮目前只显示“录制模块正在接入”，尚未调用真实录制接口。

## 本阶段计划修改
- 接入 `CameraClient.captureVideoStart()` 与 `captureVideoStop()`。
- 保存录制文件到应用专属 `Movies/UsbCapture` 目录。
- 实现录制计时器和录制状态动画联动。
- 扫描录制文件列表并展示在设置面板。
- 支持调用系统播放器打开录制文件。

## 代码前后对比

### 修改前：录制按钮占位
```kotlin
private fun handleRecordClick() {
    uiState = uiState.copy(errorMessage = "录制模块正在接入")
}
```

### 修改后预期：真实录制
```kotlin
private fun handleRecordClick() {
    if (uiState.isRecording) stopRecording() else startRecording()
}
```

## 待办清单
- 实现录制开始/停止。
- 实现录制计时。
- 实现文件列表和播放入口。
- 构建验证。

## 下一步
修改 `MainActivity.kt`，接入录制与文件管理。
