# 009-版本号与APK文件名修改前备份

## 备份时间
- 2026-06-29

## 本阶段目标
- 将 APK 版本号从 `1.0.0` 自动递增一个小版本到 `1.1.0`。
- 将 `versionCode` 从 `1` 递增到 `2`，便于 Android 系统识别为新版本。
- 将构建产物文件名从默认 `app-debug.apk` 改为 `USB采集卡.apk`。
- 重新编译生成新的 APK。

## 当前修改到哪个模块
- 模块九：版本号与 APK 文件名。

## 修改前关键代码

### 版本配置
```kotlin
defaultConfig {
    applicationId = "com.codex.usbcapture"
    minSdk = 23
    targetSdk = 36
    versionCode = 1
    versionName = "1.0.0"
}
```

### APK 文件名
```kotlin
// 未自定义 APK 输出文件名，Gradle 默认生成 app-debug.apk。
```

## 预计修改后的结构
- `versionCode = 2`
- `versionName = "1.1.0"`
- 通过 Gradle variant output 配置将 APK 文件名设为 `USB采集卡.apk`。

## 待办清单
- 修改 `app/build.gradle.kts`。
- 执行 `.\gradlew.bat :app:assembleDebug`。
- 确认新 APK 文件路径和大小。
- 清理项目根目录 `.gradle` 临时缓存。
- 生成 `进度快照/010-版本号与APK文件名.md`。
