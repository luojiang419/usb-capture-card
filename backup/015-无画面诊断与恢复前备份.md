# 015-无画面诊断与恢复前备份

## 备份时间

- 日期：2026-07-15
- 阶段：无画面诊断、权限解耦与有限自动恢复
- 源文件：`app/src/main/java/com/codex/usbcapture/MainActivity.kt`
- 修改前 SHA-256：`88006C8F63176BD5C6DE7A5F1BDFBF4E7877CDCC7F265820D72B98446DF64C0A`

## 修改前工作区说明

- 工作区包含上一阶段“USB 拔出直接回主界面”的未提交改动，本阶段在其基础上继续，不回退该改动。
- 本阶段仅记录与无画面问题直接相关的代码变化，不备份构建产物和缓存。

## 修改前关键代码

### 1. 相机和录音权限被同时作为预览前置条件

```kotlin
private val permissionLauncher = registerForActivityResult(
    ActivityResultContracts.RequestMultiplePermissions()
) { result ->
    val granted = requiredRuntimePermissions().all { result[it] == true }
    if (granted) {
        openCameraOnPermissionGranted()
    } else {
        uiState = uiState.copy(
            connectionState = DeviceConnectionState.Error,
            statusText = "缺少相机或录音权限",
            errorMessage = "缺少相机或录音权限"
        )
    }
}
```

### 2. 已授权且 Surface 可用时会再次主动打开相机

```kotlin
private fun openCameraOnPermissionGranted() {
    uiState = uiState.copy(
        connectionState = DeviceConnectionState.Connecting,
        statusText = "正在检测采集卡",
        errorMessage = null
    )
    if (::cameraView.isInitialized && cameraView.isAvailable) {
        cameraClient?.openCamera(cameraView)
        refreshRenderSize()
    }
}
```

### 3. START 状态直接当作视频画面已经可用

```kotlin
CameraStatus.START -> {
    resetPreviewFps()
    hideControlsForPreview()
    uiState.copy(
        connectionState = DeviceConnectionState.Connected,
        statusText = "视频信号已连接",
        isPreviewing = true,
        captureFps = 0,
        errorMessage = null,
        deviceName = details.deviceName,
        previewSizeText = details.previewSizeText,
        previewSizes = details.previewSizes
    )
}
```

### 4. 帧回调只统计 FPS，没有首帧和断帧诊断

```kotlin
private val previewDataCallback = object : IPreviewDataCallBack {
    override fun onPreviewData(data: ByteArray?, format: IPreviewDataCallBack.DataFormat) {
        onPreviewFrameArrived()
    }
}
```

## 本阶段预期改动

- 预览仅依赖相机权限，录音权限改为开始录制时按需申请。
- 避免冷启动已授权场景重复调用 `openCamera`。
- 将 UVC START 与真实首帧分开记录。
- 增加首帧超时、断帧检测、一次有限自动恢复和分层日志。
- 修改完成后在本文件追加实际代码前后对比与验证结果。

## 修改后关键代码

### 1. 预览与录音权限解耦

```kotlin
private val cameraPermissionLauncher = registerForActivityResult(
    ActivityResultContracts.RequestPermission()
) { granted ->
    if (granted) {
        openCameraAfterPermissionGrant()
    } else {
        uiState = uiState.copy(
            connectionState = DeviceConnectionState.Error,
            statusText = "缺少相机权限",
            errorMessage = "缺少相机权限，无法显示采集画面"
        )
    }
}

private val audioPermissionLauncher = registerForActivityResult(
    ActivityResultContracts.RequestPermission()
) { granted ->
    if (granted && uiState.isPreviewing && !uiState.isRecording) {
        startRecording()
    }
}
```

### 2. 已授权冷启动依赖 AUSBC 的 Surface 回调，避免重复打开

```kotlin
private fun ensureCameraPermission() {
    if (hasPermission(Manifest.permission.CAMERA)) {
        Log.i(TAG, "Camera permission already granted; waiting for the base surface callback")
        uiState = uiState.copy(
            statusText = "正在检测采集卡",
            diagnosticText = "应用层：相机权限已授权，等待 USB/UVC"
        )
        return
    }
    cameraPermissionLauncher.launch(Manifest.permission.CAMERA)
}
```

### 3. UVC START 与真实首帧分离

```kotlin
CameraStatus.START -> {
    resetPreviewFps()
    uiState.copy(
        connectionState = DeviceConnectionState.Connecting,
        statusText = "相机已启动，正在等待视频帧",
        isPreviewing = true,
        diagnosticText = "采集层：等待首帧（最长 5 秒）"
    )
}
```

首帧到达后才切换为已连接，并抽样计算 Y 分量平均亮度：

```kotlin
uiState = uiState.copy(
    connectionState = DeviceConnectionState.Connected,
    statusText = "视频帧已连接",
    diagnosticText = if (averageLuma <= 24) {
        "帧流已到达，但内容接近黑场（亮度 $averageLuma）"
    } else {
        "帧流正常（$format，亮度 $averageLuma）"
    }
)
```

### 4. 首帧/断帧看门狗与一次有限恢复

```kotlin
private val previewWatchdog = object : Runnable {
    override fun run() {
        if (!previewFpsEnabled) return
        val receivedFrame = firstPreviewFrameReceived
        val referenceTime = if (receivedFrame) lastPreviewFrameAt else previewFpsStartedAt
        val timeout = if (receivedFrame) FRAME_STALL_TIMEOUT_MS else FIRST_FRAME_TIMEOUT_MS
        if (referenceTime > 0L && SystemClock.elapsedRealtime() - referenceTime >= timeout) {
            handlePreviewTimeout(receivedFrame)
            return
        }
        mainHandler.postDelayed(this, PREVIEW_WATCHDOG_INTERVAL_MS)
    }
}
```

恢复通过当前分辨率触发 AUSBC 自带的关闭/延迟重启流程，单次 USB 连接最多自动执行一次，避免无限重启。

## 验证结果

- `git diff --check`：通过，无空白错误。
- `./gradlew.bat :app:compileDebugKotlin --offline`：`BUILD SUCCESSFUL`。
- 最终源码 SHA-256：`B87E00246E0A2DA6AC6ADD1AB2933A1C5137C73E1DF64FE6B70DF11B23543B53`。
- 最终 APK SHA-256：`301D96179292A800F7A246A418FAFE26A3B95D69CCFF9AA732647B1DCCFF1CA2`。
- 真机 USB/UVC 行为仍需连接实际采集卡验证。
