# 027-云端发布1.4.5待真机更新验收

## 已完成内容

- 读取最新快照 `026-固定签名版1.4.4发布完成.md`，从固定签名基线继续。
- 用户已在手机上手动安装 `1.4.4`。
- 创建阶段备份 `backup/026-固定签名自动更新端到端验证前备份.md`。
- 版本已提升为：
  - `versionCode = 9`
  - `versionName = "1.4.5"`
- README 已同步到 `1.4.5`，并修正为固定签名 Release APK 发布说明。
- 本地预检构建通过，仅用于发布前验证，没有作为 Release 资产上传。
- 提交并推送 `main`：
  - 提交：`be26da1 Release v1.4.5 for signed update validation`
- 创建并推送标签 `v1.4.5`。
- GitHub Actions 云端 Release 工作流成功：
  - Run ID：`29460660417`
  - Job ID：`87503226338`
  - 云端步骤包括签名 Secrets 校验、keystore 解码、`assembleRelease`、资产上传和 Release 发布
  - 总耗时：3 分 17 秒
- GitHub Release 已生成：
  - 名称：`USB采集卡 v1.4.5`
  - 地址：`https://github.com/luojiang419/usb-capture-card/releases/tag/v1.4.5`
  - 资产：`USB-Capture-Card-v1.4.5.apk`
  - SHA-256：`768f8c91f5ad5606459826bdc444b564451dae8cfe9398348e0336fb03b14c6a`
- 已下载并复核云端 APK：
  - APK 内版本码：`9`
  - APK 内版本名：`1.4.5`
  - 证书 SHA-256：`AD:61:03:11:19:3E:90:CD:37:E5:8D:7B:46:E3:DE:BA:A3:01:C8:8D:4E:67:B4:D9:44:0E:10:22:A5:2A:4A:B9`
  - 与 `1.4.4` 固定签名完全一致
- 匿名访问 GitHub `releases/latest` 已返回 `v1.4.5` 和正确 APK 下载地址。

## 当前修改到哪个模块

模块二十六：固定签名自动更新端到端验证。云端编译和发布已完成，当前只剩真机覆盖安装验收。

## 具体修改的代码前后对比

### 1. 应用版本

修改前：

```kotlin
versionCode = 8
versionName = "1.4.4"
```

修改后：

```kotlin
versionCode = 9
versionName = "1.4.5"
```

### 2. Release 文档

修改前：

- README 当前版本为 `1.4.4`
- 发布流程仍描述 Debug APK

修改后：

- README 当前版本为 `1.4.5`
- 发布流程描述由 GitHub Actions 使用固定证书构建 Release APK

## 验收结果

- GitHub CI 云端构建：通过。
- GitHub Release 自动发布：通过。
- 云端 APK 版本信息：通过。
- 云端 APK 与 `1.4.4` 证书一致：通过。
- 匿名最新版本接口：通过。
- 手机端 `1.4.4 -> 1.4.5` 检测、下载和覆盖安装：待用户真机确认。

## 待办清单（未完成）

- 在手机已安装的 `1.4.4` 中打开“功能 -> 检查更新”。
- 确认提示发现 `1.4.5`。
- 下载并安装更新，确认系统不再提示签名不同。
- 安装完成后确认应用版本显示为 `1.4.5`。

## 下一步要做什么

直接在手机 `1.4.4` 中执行“检查更新”并安装 `1.4.5`。如果能覆盖安装，本项目的 GitHub CI 云端构建、Release 发布和客户端自动更新链路即完成端到端验收。

## 非阻塞维护提示

GitHub Actions 本次给出了 Node.js 20 actions 将被强制运行在 Node.js 24 的弃用提示，但没有影响构建和发布成功。后续可在单独维护阶段升级对应 action 主版本。
