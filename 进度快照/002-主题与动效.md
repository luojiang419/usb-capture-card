# 002-主题与动效

## 已完成内容
- 将极简 `TextView` 状态层替换为 `ComposeView` 控制层。
- 新增 `ThemeMode`：`System`、`Light`、`Dark`，并使用 `SharedPreferences` 持久化。
- 新增 `UsbCameraUiState`、`DeviceConnectionState`、`RecordingFile` 状态类型。
- 实现 Material 3 浅色/暗黑主题。
- 实现顶部状态栏、主题切换菜单、设置面板、底部录制控制区。
- 实现连接状态光点、无信号提示、预览遮罩淡入淡出、录制按钮脉冲等基础动效。
- 构建验证通过：`.\gradlew.bat :app:assembleDebug`。

## 当前修改到哪个模块
模块二：主题与动效基础。

## 具体修改的代码前后对比

### 修改前：View 状态层
```kotlin
statusText = TextView(this).apply {
    text = "等待 USB 采集卡"
    textSize = 18f
}
root.addView(statusText, ...)
```

### 修改后：Compose 控制层
```kotlin
val composeOverlay = ComposeView(this).apply {
    setContent {
        UsbCaptureTheme(themeMode) {
            UsbCaptureScreen(
                uiState = uiState,
                themeMode = themeMode,
                showSettings = showSettings,
                onThemeModeChange = ::switchTheme,
                onRecordClick = ::handleRecordClick,
                onSettingsClick = { showSettings = !showSettings }
            )
        }
    }
}
root.addView(composeOverlay, ...)
```

### 新增：主题模式
```kotlin
enum class ThemeMode(val label: String) {
    System("跟随系统"),
    Light("浅色"),
    Dark("暗黑")
}
```

### 新增：UI 状态
```kotlin
data class UsbCameraUiState(
    val connectionState: DeviceConnectionState = DeviceConnectionState.Waiting,
    val statusText: String = "等待 USB 采集卡",
    val previewSizeText: String = "1280 x 720",
    val isPreviewing: Boolean = false,
    val isRecording: Boolean = false,
    val recordingSeconds: Long = 0L,
    val errorMessage: String? = null,
    val recordingFiles: List<RecordingFile> = emptyList()
)
```

## 待办清单（未完成）
- 模块三：完善 USB 自动连接状态、权限请求、设备信息、分辨率列表。
- 模块四：接入真实录制、计时、保存目录、录制文件列表和播放入口。
- 最终 clean 构建 APK 并输出最终快照。

## 下一步要做什么
开始模块三：USB 自动连接与预览。
