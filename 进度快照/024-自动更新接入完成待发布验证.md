# 024-自动更新接入完成待发布验证

## 已完成内容

- 读取最新快照 `023-真实版本发布完成.md`，继续在 `usb摄像头` 项目中接入应用内自动更新。
- 创建阶段备份 `backup/023-自动更新接入前备份.md`。
- 新增独立更新模块：
  - `app/src/main/java/com/codex/usbcapture/AppUpdateManager.kt`
- 新模块已实现：
  - 请求 GitHub Latest Release API
  - 解析 `tag_name`、`body`、`html_url`
  - 选择 `.apk` 安装包资产
  - 语义化版本比较
  - 下载更新 APK 到应用私有目录
- `MainActivity.kt` 已接入：
  - 启动后静默检查更新
  - 功能菜单中手动“检查更新”
  - 发现新版本后弹出更新对话框
  - 下载进度提示
  - 下载完成后拉起系统安装器
  - Android 8.0+ 未知来源安装权限申请与回跳
- `AndroidManifest.xml` 已新增权限：
  - `INTERNET`
  - `REQUEST_INSTALL_PACKAGES`
- `file_paths.xml` 已新增更新包共享路径：
  - `Download/updates/`
  - `files/updates/`
- `build.gradle.kts` 已开启：

```kotlin
buildFeatures {
    compose = true
    buildConfig = true
}
```

- 应用版本已提升到新基线：
  - `versionCode = 7`
  - `versionName = "1.4.2"`
- `README.md` 已补充应用内更新说明、权限说明以及首次切入自动更新的版本说明。

## 当前修改到哪个模块

模块二十三：Android 应用内自动更新接入，代码开发与本地编译验证已完成，等待真实发版验证。

## 具体修改的代码前后对比

### 1. 之前没有更新模块，现已拆出独立更新管理器

修改前：

```text
app/src/main/java/com/codex/usbcapture/MainActivity.kt
```

修改后新增：

```text
app/src/main/java/com/codex/usbcapture/AppUpdateManager.kt
```

### 2. 之前 Manifest 没有网络和安装更新权限

修改前：

```xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
```

修改后：

```xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
```

### 3. 之前 FileProvider 只开放录制目录

修改前：

```xml
<external-files-path
    name="usb_capture_movies"
    path="Movies/UsbCapture/" />
```

修改后：

```xml
<external-files-path
    name="usb_capture_movies"
    path="Movies/UsbCapture/" />
<external-files-path
    name="usb_capture_updates"
    path="Download/updates/" />
<files-path
    name="usb_capture_internal_updates"
    path="updates/" />
```

### 4. 之前版本仍是 1.4.1，现已升到包含更新能力的新版本

修改前：

```kotlin
versionCode = 6
versionName = "1.4.1"
```

修改后：

```kotlin
versionCode = 7
versionName = "1.4.2"
```

## 验收标准

- 项目可成功编译出 Debug APK。
- 启动应用后会静默检查 GitHub 最新 Release。
- 功能菜单可手动触发“检查更新”。
- 发现新版本后可看到版本、安装包和更新说明。
- 下载完成后可拉起系统安装器。
- Android 8.0+ 未知来源安装权限不足时可跳转系统设置并回到安装流程。

## 验证结果

- `.\gradlew.bat --no-daemon :app:assembleDebug`：通过。
- 中途发现 `BuildConfig` 未生成，已通过 `buildFeatures.buildConfig = true` 修复。
- 当前本地代码已具备发布 `1.4.2` 的条件。

## 待办清单（未完成）

- 提交并推送 `1.4.2` 自动更新代码。
- 创建并推送 `v1.4.2` 标签，发布第一版带自动更新能力的 APK。
- 使用已安装的 `1.4.2` 客户端，再发布一个更高版本（例如 `1.4.3`）做真实应用内升级验证。

## 下一步要做什么

先发布 `1.4.2`。需要特别注意：

- `1.4.1` 以及更早版本本身不包含这次新写的更新代码，因此它们不能应用内自动升级到 `1.4.2`。
- 真正的“客户端自己发现新版本并下载安装”验证，需要至少两步：
  1. 先手动安装带更新功能的 `1.4.2`
  2. 再发布一个更高版本（如 `1.4.3`），由 `1.4.2` 去检测并升级
