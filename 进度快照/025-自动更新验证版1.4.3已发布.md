# 025-自动更新验证版1.4.3已发布

## 已完成内容

- 读取最新快照 `024-自动更新接入完成待发布验证.md`，继续推进应用内更新的真实升级验证。
- 创建阶段备份 `backup/024-自动更新真实升级验证前备份.md`。
- 将应用版本从 `1.4.2` 升级到 `1.4.3`：
  - `versionCode`：`7 -> 8`
  - `versionName`：`1.4.2 -> 1.4.3`
- 同步更新 `README.md` 中的当前版本说明和当前版本能力标题。
- 本地执行 `.\gradlew.bat --no-daemon :app:assembleDebug` 验证通过。
- 提交并推送 `main` 分支提交 `80e2430`：`Release v1.4.3 for update validation`
- 创建并推送标签 `v1.4.3`
- GitHub Actions `Android Release APK` 工作流成功完成：
  - Run ID：`29430744383`
  - 完成时间：`2026-07-16 00:05`（北京时间）
- GitHub Releases 已自动生成验证版：
  - Release：`USB采集卡 v1.4.3`
  - 地址：`https://github.com/luojiang419/usb-capture-card/releases/tag/v1.4.3`
  - 资产：`USB-Capture-Card-v1.4.3.apk`
  - SHA-256：`697725c906435dfa74470a03ff93e09668d8994b8672c7073886a88af11a78a4`

## 当前修改到哪个模块

模块二十四：自动更新真实升级验证版本发布。远端 `1.4.3` 已发出，等待使用已安装 `1.4.2` 的客户端做真实检测与安装验证。

## 具体修改的代码前后对比

### 1. 应用版本号

修改前：

```kotlin
versionCode = 7
versionName = "1.4.2"
```

修改后：

```kotlin
versionCode = 8
versionName = "1.4.3"
```

### 2. README 当前版本说明

修改前：

```md
当前版本：`1.4.2`
```

修改后：

```md
当前版本：`1.4.3`
```

### 3. README 当前版本能力标题

修改前：

```md
`1.4.2` 版本包含以下重点优化：
```

修改后：

```md
`1.4.3` 版本包含以下重点优化：
```

## 验收标准

- 项目可成功编译出 Debug APK。
- `push v1.4.3` 后自动触发 GitHub Release 工作流。
- Release 页面中可看到 `USB-Capture-Card-v1.4.3.apk`。
- 已安装 `1.4.2` 的客户端启动后能够检测到 `1.4.3`。
- 客户端可下载 APK，并拉起系统安装器。

## 验证结果

- 本地 Gradle 构建：通过。
- GitHub Release 工作流：通过。
- GitHub Release 页面：已生成。
- `1.4.3` 下载地址已存在：
  - `https://github.com/luojiang419/usb-capture-card/releases/download/v1.4.3/USB-Capture-Card-v1.4.3.apk`

## 待办清单（未完成）

- 在 Android 设备上手动安装 `1.4.2`。
- 打开 `1.4.2`，验证是否自动检测到 `1.4.3`。
- 点击下载更新，确认 APK 能下载完成并弹出系统安装界面。
- 如有必要，记录未知来源安装授权与回跳行为是否正常。

## 下一步要做什么

真实设备验证顺序如下：

1. 先安装 `1.4.2`：
   - `https://github.com/luojiang419/usb-capture-card/releases/tag/v1.4.2`
2. 打开 `1.4.2`，等待它检查更新。
3. 如果弹出 `1.4.3` 更新对话框，点击下载并继续安装。
4. 安装完成后确认应用版本已经变成 `1.4.3`。

如果设备端验证通过，说明整条链路已经打通：

- `push/tag`
- GitHub Actions 自动构建
- GitHub Release 自动发布
- 客户端自动发现新版本
- 下载 APK
- 提示用户安装更新
