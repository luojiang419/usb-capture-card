# 017-横竖屏采集画面自动适配

## 已完成内容

- 读取最新快照 `016-无画面诊断与性能优化.md`，在现有未提交代码上继续，未回退前序修改。
- 审计 AUSBC 3.2.7 的 `CameraActivity`、`CameraClient`、`RenderManager` 和 `AspectRatioTextureView` 字节码。
- 确认横竖屏失效属于软件刷新缺失，不是采集卡需要重新枚举。
- 创建阶段备份 `backup/016-横竖屏渲染刷新前备份.md`。
- 修复旋转后的预览布局链路：
  - 从当前 `CameraRequest` 获取采集分辨率。
  - 重新调用 `cameraView.setAspectRatio()`，让 AUSBC 根据新方向计算宽高比。
  - 请求 `cameraContainer` 重新布局。
  - 使用 `doOnLayout` 等待 `TextureView` 新宽高生效。
  - 布局完成后调用 `cameraClient.setRenderSize()` 更新 GLES 渲染尺寸。
- 增加 `UsbCapture` 日志，记录旋转前后实际渲染宽高。
- 未修改 Manifest、UVC 分辨率、USB 权限、自动恢复和录制流程。

## 当前修改到哪个模块

模块十六：横竖屏切换时采集画面自动适配。源码和 APK 构建已完成，等待真机旋转验证。

## 根因分析

应用在 Manifest 中处理 `orientation|screenSize` 配置变化，因此旋转时 Activity 不会销毁重建，USB/UVC 会话得以保留。

AUSBC 重新插拔时会重新执行 `CameraClient.openCamera()`。该方法会调用：

```kotlin
cameraView.setAspectRatio(previewWidth, previewHeight)
```

`AspectRatioTextureView` 在横屏时使用 `width / height`，竖屏时使用 `height / width`，并通过 `requestLayout()` 更新显示 Surface。

原旋转路径只有：

```kotlin
cameraClient?.setRenderSize(cameraView.width, cameraView.height)
```

这个方法只向 `RenderManager` 发送 `MSG_GL_CHANGED_SIZE`，更新 CameraRender、ScreenRender 和 SurfaceTexture buffer 的 GLES 尺寸；它不会重新计算 `TextureView` 的方向相关宽高比。并且调用发生在配置回调中，新布局尚未完成，读取到的经常还是旋转前尺寸。因此必须重新插拔后才正常。

## 具体修改的代码前后对比

修改前：

```kotlin
private fun refreshRenderSize() {
    if (!::cameraView.isInitialized) return
    cameraView.post {
        if (cameraView.width > 0 && cameraView.height > 0) {
            cameraClient?.setRenderSize(cameraView.width, cameraView.height)
        }
    }
}
```

修改后：

```kotlin
private fun refreshRenderSize() {
    if (!::cameraView.isInitialized) return
    val client = cameraClient ?: return
    val oldWidth = cameraView.width
    val oldHeight = cameraView.height
    client.getCameraRequest()?.let { request ->
        cameraView.setAspectRatio(request.previewWidth, request.previewHeight)
    }
    cameraContainer.requestLayout()
    cameraView.doOnLayout { view ->
        if (view.width > 0 && view.height > 0) {
            Log.i(
                TAG,
                "Render layout updated for ${screenOrientation.name}: " +
                    "$oldWidth x $oldHeight -> ${view.width} x ${view.height}"
            )
            client.setRenderSize(view.width, view.height)
        }
    }
}
```

## 验收标准

- 采集画面正常显示时从竖屏切换横屏，无需重新插拔即可按横屏比例显示。
- 从横屏切回竖屏，无需重新插拔即可按竖屏比例显示。
- 连续旋转 10 次，USB 权限不重复弹出，预览帧不中断。
- 录制过程中旋转，录制状态不被停止，输出分辨率仍保持采集分辨率。
- 日志能看到 `Render layout updated for Portrait/Landscape` 和旋转前后宽高。

## 验证结果

- `git diff --check`：通过。
- `./gradlew.bat :app:compileDebugKotlin --offline`：`BUILD SUCCESSFUL`。
- `./gradlew.bat :app:testDebugUnitTest :app:assembleDebug --offline`：`BUILD SUCCESSFUL`；当前没有单元测试源码。
- APK：`app/build/outputs/apk/debug/USB采集卡.apk`。
- APK 大小：`26341540` bytes。
- APK SHA-256：`8EA7646097ACD3E3CAD0398F9432C0458265C9E57699619C87C40F65D912177F`。
- ADB 当前无连接设备，未执行真机旋转测试。
- 项目构建目录约 `0.18 GiB`，低于 20GB，无需清理构建产物。

## 待办清单（未完成）

- 真机安装最新 APK，在预览状态连续执行 10 次横竖屏切换。
- 在录制状态执行横竖屏切换，确认录制不中断且文件可播放。
- 查看 `UsbCapture` 日志，确认每次旋转后宽高确实发生变化。
- 如个别设备仍不刷新，采集该设备旋转日志，再评估是否需要针对厂商系统增加一次延迟布局兜底。

## 下一步要做什么

连接真机并安装最新 APK，保持采集卡连接后直接旋转设备。测试时可执行：

```powershell
adb logcat -v time -s UsbCapture RenderManager AspectRatioTextureView
```

正常情况下，每次横竖屏切换都应出现新的 `Render layout updated` 尺寸日志，不再需要重新插拔采集卡。
