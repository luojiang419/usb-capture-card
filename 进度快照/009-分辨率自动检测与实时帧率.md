# 009-分辨率自动检测与实时帧率

## 已完成内容
- 在采集设置的分辨率区域新增“自动检测”按钮。
- 自动检测会尝试多个比例参数读取 AUSBC 返回的可用分辨率：
  - 不指定比例。
  - `16:9`。
  - `4:3`。
  - `1:1`。
  - `5:4`。
- 检测结果会去重并按分辨率面积从高到低排序，避免只显示默认 `1280 x 720`。
- 接入 AUSBC 预览数据回调 `addPreviewDataCallBack`，按实际预览帧回调统计实时采集 FPS。
- 在采集画面右上角新增半透明实时 FPS 显示，仅预览时显示。
- 预览停止、断开、错误、销毁时会停止 FPS 统计并清零。
- 已执行构建：`.\gradlew.bat :app:assembleDebug`，构建成功。
- 最新 APK：`D:\Myproject\usb摄像头\app\build\outputs\apk\debug\app-debug.apk`
- 最新 APK 大小：`26174486` bytes。
- 项目根目录 `.gradle` 临时缓存已清理；保留 `app/build`，因为其中包含最新 APK。

## 当前修改到哪个模块
模块八：分辨率自动检测与实时 FPS 显示。

## 具体修改的代码前后对比

### 1. 分辨率读取方式

修改前：
```kotlin
val sizes = cameraClient?.getAllPreviewSizes()
    ?.map { PreviewOption(it.width, it.height) }
    ?.distinct()
    .orEmpty()
```

修改后：
```kotlin
private fun detectPreviewSizes(): List<PreviewOption> {
    val client = cameraClient ?: return emptyList()
    val sizes = linkedSetOf<PreviewOption>()
    PREVIEW_SIZE_ASPECT_RATIOS.forEach { ratio ->
        runCatching { client.getAllPreviewSizes(ratio) }
            .getOrNull()
            ?.mapTo(sizes) { PreviewOption(it.width, it.height) }
    }
    val request = client.getCameraRequest()
    if (request != null) {
        sizes.add(PreviewOption(request.previewWidth, request.previewHeight))
    }
    return sizes
        .filter { it.width > 0 && it.height > 0 }
        .sortedWith(compareByDescending<PreviewOption> { it.width * it.height }.thenByDescending { it.width })
}
```

### 2. 自动检测按钮

修改前：
```kotlin
options.forEach { option ->
    val selected = option.label == current
    OutlinedButton(
        onClick = { onResolutionSelected(option) },
        enabled = !selected,
        ...
    ) {
        Text(option.label, fontSize = 12.sp)
    }
}
```

修改后：
```kotlin
OutlinedButton(
    onClick = onAutoDetectResolutions,
    shape = RoundedCornerShape(8.dp),
    border = BorderStroke(1.dp, MaterialTheme.colorScheme.primary.copy(alpha = 0.75f)),
    contentPadding = PaddingValues(horizontal = 12.dp, vertical = 6.dp)
) {
    Text("自动检测", fontSize = 12.sp)
}
```

### 3. 实时 FPS 状态

修改前：
```kotlin
val selectedFrameRate: Int = DEFAULT_FRAME_RATE,
val isPreviewing: Boolean = false,
```

修改后：
```kotlin
val selectedFrameRate: Int = DEFAULT_FRAME_RATE,
val captureFps: Int = 0,
val isPreviewing: Boolean = false,
```

### 4. 右上角半透明 FPS 显示

修改前：
```kotlin
// 无实时采集 FPS 显示。
```

修改后：
```kotlin
CaptureFpsOverlay(uiState)
```

```kotlin
Text(
    text = "${uiState.captureFps} fps",
    color = Color.White.copy(alpha = 0.9f),
    fontSize = 13.sp,
    fontWeight = FontWeight.SemiBold,
    modifier = Modifier.padding(horizontal = 10.dp, vertical = 6.dp),
    maxLines = 1
)
```

## 待办清单（未完成）
- 真机连接 USB 采集卡验证“自动检测”是否能列出更多分辨率。
- 真机验证右上角 FPS 数值是否持续刷新，且不遮挡关键画面。
- 如果某些采集卡仍只返回 `1280 x 720`，需要继续评估 AUSBC 底层 UVC 格式/帧描述符接口。

## 下一步要做什么
安装最新 `app-debug.apk` 到真机，进入采集设置点击“自动检测”，同时观察右上角实时 FPS 显示是否符合实际采集状态。
