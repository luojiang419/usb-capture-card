# 023-真实版本发布完成

## 已完成内容

- 读取最新快照 `022-打tag自动发布Release.md`，在其基础上继续推进真实发版验证。
- 创建阶段备份 `backup/022-真实版本发布前备份.md`。
- 将应用版本从 `1.4.0` 升级到 `1.4.1`：
  - `versionCode`：`5 -> 6`
  - `versionName`：`1.4.0 -> 1.4.1`
- 同步更新 `README.md` 中的当前版本说明和版本能力标题。
- 本地执行 `.\gradlew.bat --no-daemon :app:assembleDebug` 验证通过。
- 提交并推送 `main` 分支提交 `eaf9a93`：`Release v1.4.1`
- 创建并推送标签 `v1.4.1`
- GitHub Actions `Android Release APK` 工作流成功完成：
  - Run ID：`29429015884`
  - 完成时间：`2026-07-15 23:41`（北京时间）
- GitHub Releases 已自动生成正式条目：
  - Release：`USB采集卡 v1.4.1`
  - 地址：`https://github.com/luojiang419/usb-capture-card/releases/tag/v1.4.1`
  - 资产：`USB-Capture-Card-v1.4.1.apk`

## 当前修改到哪个模块

模块二十二：真实版本递增、tag 推送与 GitHub Release 自动发布验证。已经完成闭环验证。

## 具体修改的代码前后对比

### 1. 应用版本号

修改前：

```kotlin
versionCode = 5
versionName = "1.4.0"
```

修改后：

```kotlin
versionCode = 6
versionName = "1.4.1"
```

### 2. README 当前版本说明

修改前：

```md
当前版本：`1.4.0`
```

修改后：

```md
当前版本：`1.4.1`
```

### 3. README 当前版本能力标题

修改前：

```md
`1.4.0` 版本包含以下重点优化：
```

修改后：

```md
`1.4.1` 版本包含以下重点优化：
```

## 验收标准

- `push main` 后工作流仍可正常构建。
- `push v1.4.1` 后自动触发 `Android Release APK`。
- GitHub Release 自动创建成功。
- Release 页面中可看到稳定英文命名资产 `USB-Capture-Card-v1.4.1.apk`。
- 自动发布流程无需手工上传 APK。

## 验证结果

- 本地 Gradle 构建：通过。
- GitHub Release 工作流：通过。
- GitHub Release 页面：已生成。
- 资产下载地址已存在：
  - `https://github.com/luojiang419/usb-capture-card/releases/download/v1.4.1/USB-Capture-Card-v1.4.1.apk`

## 待办清单（未完成）

- 进入 `VntcApp1.0` 项目，补齐 GitHub Actions 自动构建并发布到 Release 的链路。
- 对齐 `VntcApp1.0` 的 Release 资产命名与客户端更新选择逻辑。
- 做一次客户端真实更新校验，确认已安装客户端能检测、下载并提示更新。

## 下一步要做什么

切换到 `D:\Myproject\vnt2.0\VntcApp1.0`，先创建本阶段备份，再只做“自动发布链路”模块，不重写现有客户端更新代码，优先复用它当前已经存在的 `lib/update` 逻辑。
