# 015-USB拔出直接回主界面

## 已完成内容
- 已读取最新快照 `014-GitHub发布与README.md`，从上一阶段继续。
- 已创建修改前备份：`D:\Myproject\usb摄像头\backup\014-USB拔出回主界面前备份.md`。
- 已优化 USB 采集卡拔出/断开后的界面回退逻辑：
  - 关闭功能菜单。
  - 关闭采集设置面板。
  - 解除锁屏状态。
  - 显示主界面控制层。
  - 退出沉浸式隐藏系统栏状态。
- 已同步相机 `STOP` 和普通错误路径，避免相机停止事件覆盖成旧的控制层状态。
- 已重新构建 Debug APK：
  - 构建命令：`.\gradlew.bat :app:assembleDebug`
  - 构建结果：`BUILD SUCCESSFUL`
  - APK 路径：`D:\Myproject\usb摄像头\app\build\outputs\apk\debug\USB采集卡.apk`
  - APK 大小：`26190878` bytes
  - APK 更新时间：`2026/7/3 23:20:17`
- 已检查构建缓存：`.gradle` 约 `1695368` bytes，远低于 20GB，无需清理。

## 当前修改到哪个模块
模块十四：USB 拔出后直接回到主等待界面。

## 具体修改的代码前后对比

### 1. 相机停止状态统一回主界面

修改前：
```kotlin
CameraStatus.STOP -> {
    stopPreviewFps()
    isScreenLocked = false
    revealControls()
    uiState.copy(
        connectionState = DeviceConnectionState.Waiting,
        statusText = "等待采集卡",
        isPreviewing = false,
        isRecording = false,
        captureFps = 0,
        deviceName = details.deviceName,
        previewSizes = details.previewSizes
    )
}
```

修改后：
```kotlin
CameraStatus.STOP -> {
    stopPreviewFps()
    showMainScreenChrome()
    uiState.copy(
        connectionState = DeviceConnectionState.Waiting,
        statusText = "等待采集卡",
        isPreviewing = false,
        isRecording = false,
        captureFps = 0,
        deviceName = details.deviceName,
        previewSizes = details.previewSizes
    )
}
```

### 2. 新增主界面状态恢复函数

修改前：
```kotlin
private fun revealControls() {
    if (isScreenLocked) return
    showControls = true
    markControlsInteraction()
}
```

修改后：
```kotlin
private fun showMainScreenChrome() {
    isScreenLocked = false
    showFunctionMenu = false
    showSettings = false
    showControls = true
    markControlsInteraction()
    exitImmersiveMode()
}
```

### 3. 新增退出沉浸模式函数

修改前：
```kotlin
private fun enterImmersiveMode() {
    WindowInsetsControllerCompat(window, window.decorView).apply {
        systemBarsBehavior = WindowInsetsControllerCompat.BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE
        hide(WindowInsetsCompat.Type.systemBars())
    }
}
```

修改后：
```kotlin
private fun enterImmersiveMode() {
    WindowInsetsControllerCompat(window, window.decorView).apply {
        systemBarsBehavior = WindowInsetsControllerCompat.BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE
        hide(WindowInsetsCompat.Type.systemBars())
    }
}

private fun exitImmersiveMode() {
    WindowInsetsControllerCompat(window, window.decorView)
        .show(WindowInsetsCompat.Type.systemBars())
}
```

### 4. USB 拔出/断开回调接入统一逻辑

修改前：
```kotlin
override fun onDetachDec(device: UsbDevice?) {
    runOnUiThread {
        stopRecordingTicker()
        stopPreviewFps()
        revealControls()
        uiState = uiState.copy(
            connectionState = DeviceConnectionState.Waiting,
            statusText = "采集卡已拔出",
            deviceName = deviceLabel(device),
            isPreviewing = false,
            isRecording = false
        )
    }
}

override fun onDisConnectDec(device: UsbDevice?, ctrlBlock: USBMonitor.UsbControlBlock?) {
    runOnUiThread {
        stopRecordingTicker()
        stopPreviewFps()
        revealControls()
        uiState = uiState.copy(
            connectionState = DeviceConnectionState.Waiting,
            statusText = "采集卡连接已断开",
            deviceName = deviceLabel(device),
            isPreviewing = false,
            isRecording = false
        )
    }
}
```

修改后：
```kotlin
override fun onDetachDec(device: UsbDevice?) {
    runOnUiThread {
        stopRecordingTicker()
        stopPreviewFps()
        showMainScreenChrome()
        uiState = uiState.copy(
            connectionState = DeviceConnectionState.Waiting,
            statusText = "采集卡已拔出",
            deviceName = deviceLabel(device),
            isPreviewing = false,
            isRecording = false
        )
    }
}

override fun onDisConnectDec(device: UsbDevice?, ctrlBlock: USBMonitor.UsbControlBlock?) {
    runOnUiThread {
        stopRecordingTicker()
        stopPreviewFps()
        showMainScreenChrome()
        uiState = uiState.copy(
            connectionState = DeviceConnectionState.Waiting,
            statusText = "采集卡连接已断开",
            deviceName = deviceLabel(device),
            isPreviewing = false,
            isRecording = false
        )
    }
}
```

## 待办清单（未完成）
- 真机连接 USB 采集卡，进入预览后拔掉采集卡，确认无需多次滑动即可回到主等待界面。
- 如后续要发布给他人使用，可再做 Release 签名 APK 和 GitHub Release 更新。

## 下一步要做什么
在真机安装最新 Debug APK，实测以下场景：
- 正常预览时拔掉采集卡。
- 打开功能菜单时拔掉采集卡。
- 打开采集设置面板时拔掉采集卡。
- 锁屏状态下拔掉采集卡。
