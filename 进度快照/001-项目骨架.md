# 001-项目骨架

## 已完成内容
- 创建原生 Android Gradle 工程骨架。
- 配置 Gradle Wrapper、Android SDK 路径、Kotlin、Compose、Material 3、AUSBC 3.2.7、JitPack 与阿里云 Maven 镜像。
- 配置 USB Host、USB 插入 intent-filter、设备过滤文件、基础 AppCompat 主题。
- 创建最小 `MainActivity`，基于 AUSBC 3.2.7 的 `CameraClient + CameraUvcStrategy` 接入 UVC 采集卡。
- 首次构建验证通过：`.\gradlew.bat :app:assembleDebug`。

## 当前修改到哪个模块
模块一：项目骨架。

## 具体修改的代码前后对比

### 修改前
```text
D:\Myproject\usb摄像头
└── 空目录
```

### 修改后
```text
D:\Myproject\usb摄像头
├── app/
│   ├── build.gradle.kts
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── java/com/codex/usbcapture/MainActivity.kt
│       └── res/xml/device_filter.xml
├── gradle/wrapper/
├── build.gradle.kts
├── settings.gradle.kts
├── gradle.properties
├── local.properties
├── gradlew
└── gradlew.bat
```

### 关键新增：AUSBC 依赖
```kotlin
implementation("com.github.jiangdongguo.AndroidUSBCamera:libausbc:3.2.7")
```

### 关键新增：USB 插入配置
```xml
<intent-filter>
    <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
</intent-filter>
<meta-data
    android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
    android:resource="@xml/device_filter" />
```

### 关键新增：UVC 相机客户端
```kotlin
override fun getCameraClient(): CameraClient {
    val request = CameraRequest.Builder()
        .setPreviewWidth(1280)
        .setPreviewHeight(720)
        .create()

    return CameraClient.newBuilder(this)
        .setEnableGLES(true)
        .setRawImage(false)
        .setCameraStrategy(CameraUvcStrategy(this))
        .setCameraRequest(request)
        .setDefaultRotateType(RotateType.ANGLE_0)
        .openDebug(true)
        .build()
}
```

## 待办清单（未完成）
- 模块二：实现浅色/暗黑/跟随系统主题、现代化 Compose UI 与动效。
- 模块三：完善 USB 自动连接状态、权限、设备/分辨率信息。
- 模块四：实现录制、计时、文件保存与录制文件列表。
- 最终执行 clean 构建并确认 APK 产物路径。

## 下一步要做什么
开始模块二：主题与动效基础。
