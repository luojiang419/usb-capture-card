# 003-USB预览

## 已完成内容
- 添加运行时 `CAMERA`、`RECORD_AUDIO` 权限请求。
- 授权后调用 `openCameraOnPermissionGranted()` 重新打开 UVC 预览。
- 保存 `CameraClient` 与 `CameraUvcStrategy` 引用，用于读取设备和分辨率。
- 接入 `IDeviceConnectCallBack`，更新 USB 插入、拔出、授权、取消授权等 UI 状态。
- 设置面板新增分辨率列表，并支持调用 `cameraClient.updateResolution(width, height)` 切换分辨率。
- 显式加入 `libuvc:3.2.7` 编译依赖，解决回调中 `USBMonitor` 类型不可见问题。
- 构建验证通过：`.\gradlew.bat :app:assembleDebug`。

## 当前修改到哪个模块
模块三：USB 自动连接与预览。

## 具体修改的代码前后对比

### 修改前：只创建 UVC 策略
```kotlin
return CameraClient.newBuilder(this)
    .setCameraStrategy(CameraUvcStrategy(this))
    .build()
```

### 修改后：保存策略并监听设备状态
```kotlin
val strategy = CameraUvcStrategy(this).also { strategy ->
    uvcStrategy = strategy
    strategy.setDeviceConnectStatusListener(createDeviceConnectCallback())
}

return CameraClient.newBuilder(this)
    .setCameraStrategy(strategy)
    .build()
    .also { cameraClient = it }
```

### 新增：运行时权限
```kotlin
private val permissionLauncher = registerForActivityResult(
    ActivityResultContracts.RequestMultiplePermissions()
) { result ->
    val granted = requiredRuntimePermissions().all { result[it] == true }
    if (granted) openCameraOnPermissionGranted()
}
```

### 新增：分辨率切换
```kotlin
private fun updateResolution(option: PreviewOption) {
    val changed = cameraClient?.updateResolution(option.width, option.height) == true
    uiState = uiState.copy(
        statusText = if (changed) "正在切换分辨率" else uiState.statusText,
        previewSizeText = option.label
    )
}
```

## 待办清单（未完成）
- 模块四：接入真实录制开始/停止。
- 模块四：实现录制计时。
- 模块四：保存到应用专属 `Movies/UsbCapture` 目录。
- 模块四：展示录制文件列表并支持系统播放器打开。
- 最终执行 clean 构建并输出 APK 完成快照。

## 下一步要做什么
开始模块四：录制与文件。
