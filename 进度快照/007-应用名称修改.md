# 007-应用名称修改

## 已完成内容
- 将 APK 安装后的软件显示名称从 `USB Capture` 修改为 `USB采集卡`。
- 修改位置：`app/src/main/res/values/strings.xml`。
- Manifest 已使用 `android:label="@string/app_name"`，因此安装后的桌面/系统应用名称会跟随该字符串。
- 重新执行最终构建：`.\gradlew.bat clean :app:assembleDebug`，构建成功。
- 最新 APK：`D:\Myproject\usb摄像头\app\build\outputs\apk\debug\app-debug.apk`
- 最新 APK 大小：`26174488` bytes。
- 项目 `.gradle` 临时缓存已清理；保留 `app/build`，因为其中包含最新 APK。

## 当前修改到哪个模块
模块六：应用名称修改。

## 具体修改的代码前后对比

### 修改前
```xml
<string name="app_name">USB Capture</string>
```

### 修改后
```xml
<string name="app_name">USB采集卡</string>
```

## 待办清单（未完成）
- 真机安装最新 APK 后确认桌面图标和系统应用列表显示为 `USB采集卡`。

## 下一步要做什么
安装最新 `app-debug.apk` 到真机验证应用名称显示。
