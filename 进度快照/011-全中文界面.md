# 011-全中文界面

## 已完成内容
- 修复 AUSBC 底层英文错误信息原样显示的问题。
- 将截图中的英文错误 `Find no uvc devices...` 映射为中文提示：`未发现采集卡，请连接采集卡后重试`。
- 将默认设备名从 `USB/UVC` 改为中文：
  - 未连接：`采集卡`
  - 已连接：`采集卡已连接`
- 将界面状态中的 `USB 采集卡` 类文本统一调整为中文表达，例如 `等待采集卡`、`正在检测采集卡`。
- 将帧率显示从 `fps` 改为 `帧/秒`。
- 将分辨率分隔符从 `x` 改为 `×`。
- 将录制文件显示名隐藏 `.mp4` 扩展名，避免文件列表出现英文扩展名。
- 将文件大小单位从 `KB/MB` 改为 `千字节/兆字节`。
- 录制文件名前缀从 `USB_CAPTURE_` 改为 `采集录像_`。
- 按发布规则将版本从 `1.1.0` 升级到 `1.2.0`，`versionCode` 从 `2` 升到 `3`。
- 已执行构建：`.\gradlew.bat :app:assembleDebug`，构建成功。
- 最新 APK：`D:\Myproject\usb摄像头\app\build\outputs\apk\debug\USB采集卡.apk`
- 最新 APK 大小：`26174490` bytes。
- 构建元数据确认：
  - `versionCode`: `3`
  - `versionName`: `1.2.0`
  - `outputFile`: `USB采集卡.apk`
- 项目根目录 `.gradle` 临时缓存已清理；保留 `app/build`，因为其中包含最新 APK。

## 当前修改到哪个模块
模块十：全中文界面。

## 具体修改的代码前后对比

### 1. 底层英文错误中文化

修改前：
```kotlin
errorMessage = status.message
```

修改后：
```kotlin
errorMessage = localizeCameraMessage(status.message, "正在匹配可用分辨率")
```

新增：
```kotlin
private fun localizeCameraMessage(message: String?, fallback: String): String {
    val text = message?.trim().orEmpty()
    if (text.isBlank()) return fallback
    val lower = text.lowercase(Locale.US)
    return when {
        lower.contains("find no uvc devices") || lower.contains("getusbdevicelist") -> {
            "未发现采集卡，请连接采集卡后重试"
        }
        lower.contains("permission") -> "缺少采集卡或相机权限"
        lower.contains("preview") && lower.contains("size") -> "当前分辨率不可用，请自动检测后重新选择"
        lower.contains("open") && lower.contains("camera") -> "打开采集画面失败"
        lower.contains("disconnect") || lower.contains("detach") -> "采集卡连接已断开"
        lower.contains("error") || lower.any { it in 'a'..'z' } -> fallback
        else -> text
    }
}
```

### 2. 默认设备名

修改前：
```kotlin
if (device == null) return "USB/UVC"
return device.productName ?: "USB/UVC"
```

修改后：
```kotlin
return if (device == null) "采集卡" else "采集卡已连接"
```

### 3. 帧率显示

修改前：
```kotlin
text = "${uiState.captureFps} fps"
Text("${frameRate}fps", fontSize = 12.sp)
```

修改后：
```kotlin
text = "${uiState.captureFps} 帧/秒"
Text("${frameRate}帧/秒", fontSize = 12.sp)
```

### 4. 版本号

修改前：
```kotlin
versionCode = 2
versionName = "1.1.0"
```

修改后：
```kotlin
versionCode = 3
versionName = "1.2.0"
```

## 待办清单（未完成）
- 真机安装 `USB采集卡.apk`，确认无采集卡时不再显示英文错误。
- 真机检查采集设置、帧率显示、录制文件列表等界面是否仍有英文。

## 下一步要做什么
安装最新 `D:\Myproject\usb摄像头\app\build\outputs\apk\debug\USB采集卡.apk` 到真机，复现无采集卡场景，确认提示显示为中文。
