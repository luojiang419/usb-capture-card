# 013-等待状态UI精简

## 已完成内容
- 已读取最新快照 `012-左上功能按钮与锁屏防误触.md`，从上一阶段继续。
- 已创建修改前备份：`D:\Myproject\usb摄像头\backup\012-等待状态UI精简前备份.md`。
- 移除左上角独立状态胶囊，未连接时不再出现额外的“等待采集卡”提示块。
- 左上角保留功能按钮，按钮文字固定为“功能”。
- 功能按钮内新增连接状态指示灯：
  - 等待：弱显示。
  - 连接中：动态显示。
  - 已连接：使用主色。
  - 错误：使用错误色。
- 中心等待态改为占满屏幕居中显示。
- 未预览/等待状态下不再显示“采集卡已连接 1280 × 720”这类误导副标题。
- 按发布规则将版本从 `1.3.0` 升级到 `1.4.0`，`versionCode` 从 `4` 升到 `5`。
- 已执行构建：`.\gradlew.bat :app:assembleDebug`，构建成功。
- 最新 APK：`D:\Myproject\usb摄像头\app\build\outputs\apk\debug\USB采集卡.apk`
- 最新 APK 大小：`26190879` bytes。
- 构建元数据确认：
  - `versionCode`: `5`
  - `versionName`: `1.4.0`
  - `outputFile`: `USB采集卡.apk`
- 项目根目录 `.gradle` 临时缓存已清理；保留 `app/build`，因为其中包含最新 APK。

## 当前修改到哪个模块
模块十二：等待状态 UI 精简。

## 具体修改的代码前后对比

### 1. 移除左上角独立状态胶囊

修改前：
```kotlin
Column(
    horizontalAlignment = Alignment.Start,
    verticalArrangement = Arrangement.spacedBy(10.dp)
) {
    if (!uiState.isPreviewing || uiState.errorMessage != null) {
        StatusPill(uiState)
    }
    FunctionMenuButton(
        uiState = uiState,
        expanded = functionMenuExpanded,
        onExpandedChange = onFunctionMenuExpandedChange,
        onRecordClick = onRecordClick,
        onSettingsClick = onSettingsClick,
        onExitClick = onExitClick
    )
}
```

修改后：
```kotlin
FunctionMenuButton(
    uiState = uiState,
    expanded = functionMenuExpanded,
    onExpandedChange = onFunctionMenuExpandedChange,
    onRecordClick = onRecordClick,
    onSettingsClick = onSettingsClick,
    onExitClick = onExitClick
)
```

### 2. 功能按钮增加状态指示灯

修改前：
```kotlin
val buttonText = if (uiState.isPreviewing) "功能" else "等待采集卡"
```

修改后：
```kotlin
val buttonText = "功能"
val targetColor = when (uiState.connectionState) {
    DeviceConnectionState.Connected -> MaterialTheme.colorScheme.primary
    DeviceConnectionState.Connecting -> MaterialTheme.colorScheme.secondary
    DeviceConnectionState.Error -> MaterialTheme.colorScheme.tertiary
    DeviceConnectionState.Waiting -> MaterialTheme.colorScheme.surfaceVariant
}
val color by animateColorAsState(targetColor, label = "function-status-color")
```

按钮内容修改后：
```kotlin
ConnectionDot(uiState.connectionState, color)
Spacer(Modifier.width(8.dp))
Icon(Icons.Rounded.Usb, contentDescription = buttonText, modifier = Modifier.size(18.dp))
Spacer(Modifier.width(8.dp))
Text(buttonText, maxLines = 1)
Spacer(Modifier.width(4.dp))
Icon(Icons.Rounded.KeyboardArrowDown, contentDescription = "展开", modifier = Modifier.size(18.dp))
```

### 3. 中心等待态取消误导副标题并真正居中

修改前：
```kotlin
Column(
    modifier = Modifier.fillMaxWidth(),
    horizontalAlignment = Alignment.CenterHorizontally,
    verticalArrangement = Arrangement.Center
)
```

修改后：
```kotlin
Column(
    modifier = Modifier
        .fillMaxSize()
        .padding(horizontal = 32.dp),
    horizontalAlignment = Alignment.CenterHorizontally,
    verticalArrangement = Arrangement.Center
)
```

修改前：
```kotlin
Text(
    text = "${uiState.deviceName}  ${uiState.previewSizeText}",
    color = Color.White.copy(alpha = 0.72f),
    fontSize = 13.sp
)
```

修改后：
```kotlin
val detailText = if (uiState.isPreviewing && uiState.errorMessage != null) {
    "${uiState.deviceName}  ${uiState.previewSizeText}"
} else {
    null
}
```

### 4. 删除无用状态胶囊组件

修改前：
```kotlin
private fun StatusPill(uiState: UsbCameraUiState) { ... }
```

修改后：
```kotlin
// 已删除 StatusPill，保留 ConnectionDot 给功能按钮复用。
```

### 5. 版本号

修改前：
```kotlin
versionCode = 4
versionName = "1.3.0"
```

修改后：
```kotlin
versionCode = 5
versionName = "1.4.0"
```

## 待办清单（未完成）
- 真机验证未连接采集卡时，屏幕中心只显示 USB 图标和“等待采集卡”。
- 真机验证左上角只保留“功能”按钮，不再出现独立状态胶囊。
- 真机验证功能按钮状态指示灯在等待、连接中、已连接、错误状态下颜色变化正常。
- 真机验证功能菜单、设置、录制、退出采集画面入口仍可正常打开。

## 下一步要做什么
安装最新 `D:\Myproject\usb摄像头\app\build\outputs\apk\debug\USB采集卡.apk` 到真机，验证等待页 UI 精简效果和功能按钮状态指示灯。
