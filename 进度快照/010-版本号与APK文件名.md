# 010-版本号与APK文件名

## 已完成内容
- 将 APK 版本号从 `1.0.0` 递增到 `1.1.0`。
- 将 `versionCode` 从 `1` 递增到 `2`，便于 Android 系统识别为新安装版本。
- 将 debug APK 输出文件名从默认 `app-debug.apk` 改为 `USB采集卡.apk`。
- 已执行构建：`.\gradlew.bat :app:assembleDebug`，构建成功。
- 最新 APK：`D:\Myproject\usb摄像头\app\build\outputs\apk\debug\USB采集卡.apk`
- 最新 APK 大小：`26174490` bytes。
- 构建元数据确认：
  - `versionCode`: `2`
  - `versionName`: `1.1.0`
  - `outputFile`: `USB采集卡.apk`
- 项目根目录 `.gradle` 临时缓存已清理；保留 `app/build`，因为其中包含最新 APK。

## 当前修改到哪个模块
模块九：版本号与 APK 文件名。

## 具体修改的代码前后对比

### 1. 版本号

修改前：
```kotlin
defaultConfig {
    applicationId = "com.codex.usbcapture"
    minSdk = 23
    targetSdk = 36
    versionCode = 1
    versionName = "1.0.0"
}
```

修改后：
```kotlin
defaultConfig {
    applicationId = "com.codex.usbcapture"
    minSdk = 23
    targetSdk = 36
    versionCode = 2
    versionName = "1.1.0"
}
```

### 2. APK 文件名

修改前：
```kotlin
// 未自定义 APK 输出文件名，Gradle 默认生成 app-debug.apk。
```

修改后：
```kotlin
applicationVariants.all {
    outputs.all {
        (this as BaseVariantOutputImpl).outputFileName = "USB采集卡.apk"
    }
}
```

## 待办清单（未完成）
- 真机安装 `USB采集卡.apk`，确认系统识别版本为 `1.1.0`。
- 后续每次重新发布 APK 前继续递增版本号，避免 Android 覆盖安装时版本不变。

## 下一步要做什么
安装最新 `D:\Myproject\usb摄像头\app\build\outputs\apk\debug\USB采集卡.apk` 到真机验证版本号和文件名。
