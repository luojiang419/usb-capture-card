# 026-固定签名版1.4.4发布完成

## 已完成内容

- 读取最新快照 `025-自动更新验证版1.4.3已发布.md`，继续修复“更新时提示签名不同”的问题。
- 创建阶段备份 `backup/025-固定签名自动更新修复前备份.md`。
- 根因已确认：
  - 仓库原先的 Release 工作流构建的是 `Debug APK`
  - GitHub Actions 每次在临时 runner 上生成新的默认 debug keystore
  - 导致 `1.4.2`、`1.4.3` 的证书不一致，Android 安装器拒绝覆盖更新
- 本机稳定 keystore 已确认可用：
  - 路径：`C:\Users\Administrator\.android\debug.keystore`
  - SHA-256：`AD:61:03:11:19:3E:90:CD:37:E5:8D:7B:46:E3:DE:BA:A3:01:C8:8D:4E:67:B4:D9:44:0E:10:22:A5:2A:4A:B9`
- `app/build.gradle.kts` 已接入固定签名能力：
  - 支持通过环境变量读取 keystore 路径、密码、别名
  - `release` 构建改为使用固定 signing config
- `.github/workflows/android-release-apk.yml` 已改为：
  - 校验签名 secrets
  - 解码 keystore
  - 执行 `:app:assembleRelease`
  - 从 `app/build/outputs/apk/release/USB采集卡.apk` 发布到 GitHub Release
- GitHub Secrets 已写入：
  - `USB_CAPTURE_ANDROID_KEYSTORE_BASE64`
  - `USB_CAPTURE_ANDROID_KEYSTORE_PASSWORD`
  - `USB_CAPTURE_ANDROID_KEY_ALIAS`
  - `USB_CAPTURE_ANDROID_KEY_PASSWORD`
- 版本已提升到新的固定签名基线：
  - `versionCode = 9`
  - `versionName = "1.4.4"`
- 本地 `assembleRelease` 构建已通过。
- 提交并推送 `main` 分支提交 `29e0e0a`：`Fix release signing for v1.4.4`
- 创建并推送标签 `v1.4.4`
- GitHub Actions `Android Release APK` 工作流成功完成：
  - Run ID：`29432741902`
  - 完成时间：`2026-07-16 00:34`（北京时间）
- GitHub Releases 已自动生成：
  - Release：`USB采集卡 v1.4.4`
  - 地址：`https://github.com/luojiang419/usb-capture-card/releases/tag/v1.4.4`
  - 资产：`USB-Capture-Card-v1.4.4.apk`
  - SHA-256：`f146d0b82d91db31eeaeacc9ef042aace28ab4084bbcfcfa9132bc86d07470b9`
- 远端 `v1.4.4` APK 证书已复核，和本机固定 keystore 完全一致：
  - SHA-256：`AD:61:03:11:19:3E:90:CD:37:E5:8D:7B:46:E3:DE:BA:A3:01:C8:8D:4E:67:B4:D9:44:0E:10:22:A5:2A:4A:B9`

## 当前修改到哪个模块

模块二十五：固定签名自动更新修复。CI 固定签名、Release 发布和签名复核已经完成。

## 具体修改的代码前后对比

### 1. 版本号

修改前：

```kotlin
versionCode = 8
versionName = "1.4.3"
```

修改后：

```kotlin
versionCode = 9
versionName = "1.4.4"
```

### 2. Release 工作流

修改前：

```bash
./gradlew --no-daemon :app:assembleDebug
cp "app/build/outputs/apk/debug/USB采集卡.apk" "${RELEASE_APK_PATH}"
```

修改后：

```bash
./gradlew --no-daemon :app:assembleRelease
cp "app/build/outputs/apk/release/USB采集卡.apk" "${RELEASE_APK_PATH}"
```

同时新增：

- 签名 secrets 校验
- keystore base64 解码
- `ANDROID_SIGNING_*` 环境变量注入

### 3. Gradle 固定签名

修改前：

- 没有自定义 release signing config

修改后：

- 新增环境变量驱动的固定 signing config
- `release` buildType 统一走固定签名

## 验收标准

- 本地 `assembleRelease` 能成功构建。
- GitHub Release 工作流能成功发布 `release APK`。
- 远端 APK 签名与固定 keystore 一致。
- 后续版本可以持续使用同一证书。

## 验证结果

- 本地 `assembleRelease`：通过。
- GitHub Release 工作流：通过。
- 远端 `v1.4.4` APK 证书与本机固定 keystore 指纹一致：通过。

## 待办清单（未完成）

- 在手机上卸载当前已安装的 `1.4.2` / `1.4.3` 历史测试版。
- 手动安装 `1.4.4`，作为新的稳定签名升级基线。
- 再发布一个更高版本（例如 `1.4.5`），验证 `1.4.4 -> 1.4.5` 的自动更新是否可正常覆盖安装。

## 下一步要做什么

需要特别注意：

- `1.4.2`、`1.4.3` 的签名私钥并不在当前环境中，无法再生成与它们完全一致的新包。
- 所以这两个旧测试版不能直接升级到 `1.4.4`，必须人工过渡一次。

真实验证顺序：

1. 卸载手机里当前的旧测试版 `1.4.2` / `1.4.3`
2. 手动安装 `v1.4.4`
3. 如果要验证“真正自动升级”，下一步应再发布 `v1.4.5`
4. 用已安装的 `1.4.4` 去检测并升级到 `1.4.5`
